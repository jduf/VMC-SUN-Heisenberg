

combination
- container : store any type of data
- diffdir : vimdiff over many directory
- directory : allow to search for a file in directories
- factorial : implementation of a factorial function (actually, used to test Chrono.hpp)
fit
- gnuplot : write an gnuplot script
- header : allows to write a header(footer) in a jdbin file
interpolation
iofiles
- jdtools : allows to find, process... jdbin files
- linux :  allows to lunch system command
list
- matrix-lapack : implémente une classe matrice comme étant un tableau 1D, l'accès à un élément se fait par a(i,j), utilise des routines lapack
miscellaneous
multi-dim-array
- parseur : récupère les arguments du main et fourni une méthode d'assertion de valeur
- prime : génère les n premiers nombres premier
- pstricks2pdf : create an pstriks figure
pso
- rand : random numbers generator provided by Sandro Sorella
- read-write : gère les entrées sorties vers des fichier binaire ou text (provides a way to store data in a jdbin format that allows me to use jdtools)
- rewrite : rewrite a file created with Write.hpp
- rst : provides tools to write a rst file within c++
- run : allow to run a software exactly n times when it is launched N>n time
sampling
- time : class that handle the time, chrono
test-blas
tofraction
- young : compute the product of Young tableaus


